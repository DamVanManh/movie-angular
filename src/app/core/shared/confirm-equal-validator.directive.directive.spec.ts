import { ConfirmEqualValidator.DirectiveDirective } from './confirm-equal-validator.directive.directive';

describe('ConfirmEqualValidator.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEqualValidator.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
